<Window x:Class="loork_gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:loork_gui"
        mc:Ignorable="d"
        Title="MainWindow" SizeToContent="WidthAndHeight">
  <Grid UseLayoutRounding="True" Margin="10">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Image Source="{Binding SurfaceVM.Image}" Stretch="None" Width="400" Height="240" />
    <StackPanel Grid.Column="1" Orientation="Vertical">
      <Button Width="60" Height="38" Margin="5" />
      <Button Width="60" Height="38" Margin="5" />
      <Button Width="60" Height="38" Margin="5" />
      <Button Width="60" Height="38" Margin="5" />
      <Button Width="60" Height="38" Margin="5" />
    </StackPanel>
    <Slider Grid.Column="2" Margin="10" Orientation="Vertical"
            Minimum="0" Maximum="100" SmallChange="1" LargeChange="10" Value="{Binding TriggerPercent}" />
  </Grid>
</Window>
